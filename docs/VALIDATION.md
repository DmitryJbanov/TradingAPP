# Проверки поставки

## Дополнение Sonarlab Order Blocks — 9 сентября 2026

Пройдены `npm run typecheck`, все 23 теста `npm run test:core` (7 новых для Sonarlab), `npm run build:standalone` и `npm run test:smoke`. ESLint новых TypeScript-файлов проходит без замечаний. В Windows запуск дочерних процессов esbuild/Vite потребовал выполнения за пределами песочницы. Предупреждение Vite о bundle больше 500 KB сохраняется: JS около 576 KB, gzip 180 KB.

В headless Chrome на локальном DEMO проверены добавление Sonarlab вместе с VMC и фактические вызовы рисования зон на Canvas, отмена и Escape, валидация чувствительности, применение, сброс черновика, исходные цвета, видимость по таймфреймам, включение/выключение, несколько экземпляров, изменение порядка, сохранение после перезагрузки и удаление. Исключений выполнения в браузере нет. Скриншот обоих индикаторов проверен визуально.

Hosted Worker build для этого дополнения не запускался. Публикация не выполнялась; сверка с экспортом оригинала TradingView не выполнялась. [Формулы и ограничения переноса](INDICATOR_ORDER_BLOCKS.md).

## Дополнение VMC Cipher B — 7 сентября 2026

Для патча VMC выполнены TypeScript typecheck, 16 автоматических тестов (11 новых и 5 исходных), standalone build, Node HTTP smoke и hosted Worker build. В том числе проверены вручную рассчитанные векторы WT, MFI, RSI, Schaff; обработка `na`; соответствие набора из 84 inputs исходнику; регулярная/скрытая дивергенция и задержка подтверждения; HTF/LTF merge, lookahead и закрытые свечи; Heikin Ashi; отсутствие MTF-истории; новые API-интервалы. Custom renderer вызывается с тестовым Canvas-контекстом: проверяются отсутствие исключений и неконечных координат на данных с прогревом и сигналами.

Патч проверяется на чистой копии исходного коммита с `git apply --check`, применяется, затем содержимое затронутых файлов побайтово сравнивается с результатом разработки. Обратное применение также проверяется. Документация применения находится в [APPLY_VMC_PATCH.md](APPLY_VMC_PATCH.md).

Браузерные проверки окна настроек и перетаскивания панелей не выполнялись. Сверка с экспортом значений TradingView не выполнена, численное совпадение с его историей не заявлено. Особенности переноса и ограничения истории описаны в [INDICATOR_VMC.md](INDICATOR_VMC.md). Патч не публиковался на существующий сайт. Standalone bundle после VMC: около 566 KB JS без gzip, 178 KB gzip; предупреждение о размере bundle не блокирует сборку.

## Исходная поставка

Проверки выполнены 6 сентября 2026 UTC.

| Проверка                       | Результат                                                                                         |
| ------------------------------ | ------------------------------------------------------------------------------------------------- |
| `npm run typecheck`            | Успешно, TypeScript без ошибок                                                                    |
| `npm run test:core`            | 5/5 тестов успешно                                                                                |
| `npm run build:standalone`     | Успешно, готовый Node + frontend release                                                          |
| Hosted Worker build            | Успешно, маршруты `/`, `/pair/:symbol`, четыре API                                                |
| Node HTTP smoke в DEMO         | Успешно: root, direct pair URL, assets, 404, health, 44 котировки, 400 свечей, invalid symbol 400 |
| Server rendering hosted HTML   | Успешно, root вернул HTTP 200 HTML                                                                |
| Реальный Binance запрос свечей | Успешно, BTCUSDT 4h, 400 свечей, source=live                                                      |

## Что проверяют core-тесты

1. OHLC high/low-инварианты, временные интервалы, одинаковая текущая demo-цена между всеми таймфреймами.
2. Формула отклонения от цены курсора, знак и защита от деления на ноль.
3. Разные UTC-положения лондонской сессии зимой/летом, нью-йоркский DST, выходные.
4. Валидация symbol/interval, запрет POST, неизвестный маршрут, provenance демоданных.
5. Нормализация успешного ответа Binance, отсутствие ложной метки live на остальных инструментах, HTTP 429 → demo fallback.

## Границы подтверждения

Проверка реального внешнего API отражает конкретный момент и среду; дальнейшая доступность определяется провайдером и сетью. В одном проверочном запуске общий ticker-запрос не успел/не удался и вернул DEMO, при этом свечи были live. Запрос котировок затем сокращён до каталога из 30 символов, но в повторном запуске таблица также перешла в DEMO; live ticker в этой среде не подтверждён. Приложение допускает разные статусы источника у таблицы и графика и показывает их отдельно.

Twelve Data не проверялся с настоящим ключом: ключ не предоставлен. Контейнер Docker в этой среде не запускался; автономный Node runtime и его HTTP API проверены непосредственно. Браузерные/E2E проверки, перетаскивание мышью, визуальная проверка разных экранов и автоматический аудит доступности не выполнялись. Масштабирование и crosshair настроены через API библиотеки и проверены статически/сборкой.

В standalone build есть предупреждение о размере главного JS bundle (~533 KB без gzip, ~168 KB gzip). Это не ошибка сборки; дальнейшее разделение chart-модуля возможно для оптимизации первой загрузки.

Команды для повторения:

```bash
npm ci
npm run typecheck
npm run test:core
npm run build:standalone
npm run test:smoke
```

Для проверки с реальными сетевыми источниками в bash:

```bash
SMOKE_LIVE=1 npm run test:smoke
```

Smoke-test запускает временный сервер на 127.0.0.1:3093 и завершает его после проверки. Внешняя проверка не требует ключа Binance, не совершает торговых операций.
